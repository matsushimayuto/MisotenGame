using UnityEngine;

public class Select : MonoBehaviour
{
    void Start()
    {
        //// フォルダ内のステージcsvを読み込んで、配列に格納しておく関数
        //void LoadAllStages()
        //{
        //    // Stageフォルダ内のcsvを全取得
        //    TextAsset[] stageFiles = Resources.LoadAll<TextAsset>(StageFolderPath);

        //    foreach (var csv in stageFiles)
        //    {
        //        Debug.Log($"読み込み: {csv.name}");

        //        string csvText = csv.text;
        //        ParseCSV(csv.name, csvText);
        //    }
        //}

        //void ParseCSV(string csvName, string csvText)
        //{

        //}

    }

    void Update()
    {
        //switch()
        //{
        //    case 0:
        //        break;
        //}
    }
}
